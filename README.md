# DHCP-server-installation-in-Cisco-
This project demonstrates the installation and configuration of a DHCP (Dynamic Host Configuration Protocol) server using ISC DHCP on Ubuntu Linux. The server dynamically assigns IP addresses, subnet mask, gateway, and DNS settings to client systems. The project includes installation steps, configuration, service management. 
